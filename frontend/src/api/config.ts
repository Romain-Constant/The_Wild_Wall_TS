// Define a constant baseUrl using Vite's environment variable for the backend URL
// VITE_BACKEND_URL is provided by Vite to access environment variables during development
export const baseUrl = import.meta.env.VITE_BACKEND_URL;
